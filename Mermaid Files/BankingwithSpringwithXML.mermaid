flowchart TB
  subgraph utils["com.cognizant.banking.utils"]
    CU["CustomerApp\n(main entrypoint)"]
  end

  subgraph resources["src/main/resources"]
    XML["banking-app.xml\n(Spring XML config)"]
  end

  subgraph modules["com.cognizant.banking.modules"]
    CUST["Customer (POJO)"]
    IND["Individual (extends Customer)"]
    CORP["Corporate (extends Customer)"]
    NAME["FullName (value object)"]
    ADDR["Address (value object)"]
    GEND["Gender (enum)"]
    CTYP["CompanyType (enum)"]
  end

  CU -- "loads" --> XML
  CU -- "creates" --> BF[DefaultListableBeanFactory]
  BF -- "uses" --> XBR[XmlBeanDefinitionReader]
  XBR -- "reads" --> XML
  BF -- "provides" --> BEANS["(Bean Registry)"]

  BEANS -. defines .-> CUST
  BEANS -. defines .-> IND
  BEANS -. defines .-> CORP
  BEANS -. defines .-> NAME
  BEANS -. defines .-> ADDR
  BEANS -. defines .-> GEND
  BEANS -. defines .-> CTYP

  CU -- "getBean(...)" --> BEANS
